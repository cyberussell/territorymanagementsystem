// Next.js replaces `server-only` imports at build time via a special bundler
// loader (throwing only if the module accidentally reaches a client bundle).
// Vitest has no such loader, so this stub stands in for it in tests.
export {}
